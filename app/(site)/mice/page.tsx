import type { Metadata } from "next";
import { MicePage } from "@/components/pages/MicePage";

export const metadata: Metadata = {
  title: "MICE Services",
  description:
    "Elite meetings, incentives, conferences, exhibitions, executive coordination, and destination management for global corporations.",
};

export default function Page() {
  // return <MicePage />;
}
